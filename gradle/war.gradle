apply plugin: "war"

bootWar {
    mainClassName = "pt.up.hs.pbanalysis.PbanalysisApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
